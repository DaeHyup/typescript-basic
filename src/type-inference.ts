/* eslint-disable */

let inferredCityName = 'London';
inferredCityName = 'Liverpool';

let inferredCityPopulation = 9000000;
inferredCityPopulation = 5000000;

let inferredIsCaptial = true;
inferredIsCaptial = false;
